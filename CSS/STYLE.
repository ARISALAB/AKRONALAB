/* ==========================================================================
   AR AKRON SERVICES — stylesheet
   Design language: editorial consulting studio with a Greek hospitality
   accent. Signature device: services presented as a numbered "menu" of
   courses, echoing the restaurant/hospitality world the firm serves.
   ========================================================================== */

:root{
  /* Color */
  --ink:        #1c1f16;
  --ink-soft:   #3a3f31;
  --olive:      #3f4a34;
  --olive-dark: #2b3324;
  --stone:      #efeade;
  --stone-deep: #e4ddcc;
  --cream:      #f8f6f0;
  --brass:      #b8874a;
  --brass-dark: #9c6f39;
  --wine:       #7a3229;
  --line:       rgba(28,31,22,0.14);
  --line-soft:  rgba(28,31,22,0.08);

  /* Type */
  --font-display: "Fraunces", "Iowan Old Style", "Palatino Linotype", serif;
  --font-body: "Inter", "Segoe UI", system-ui, -apple-system, sans-serif;

  /* Scale */
  --container: 1180px;
  --radius: 2px;
}

*,*::before,*::after{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
@media (prefers-reduced-motion: reduce){
  html{ scroll-behavior:auto; }
  *,*::before,*::after{ animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important; scroll-behavior:auto !important; }
}

body{
  margin:0;
  background:var(--stone);
  color:var(--ink);
  font-family:var(--font-body);
  font-size:16px;
  line-height:1.6;
  -webkit-font-smoothing:antialiased;
}

img{ max-width:100%; display:block; }

a{ color:inherit; text-decoration:none; }

h1,h2,h3,h4{
  font-family:var(--font-display);
  font-weight:600;
  line-height:1.08;
  margin:0 0 0.5em;
  letter-spacing:-0.01em;
  color:var(--ink);
}

h1{ font-size:clamp(2.4rem, 5.4vw, 4.4rem); font-weight:500; }
h2{ font-size:clamp(1.8rem, 3.4vw, 2.7rem); font-weight:500; }
h3{ font-size:1.35rem; font-weight:600; }

p{ margin:0 0 1.1em; color:var(--ink-soft); }

.eyebrow{
  font-family:var(--font-body);
  font-size:0.72rem;
  font-weight:600;
  letter-spacing:0.18em;
  text-transform:uppercase;
  color:var(--brass-dark);
  display:flex;
  align-items:center;
  gap:0.6em;
  margin-bottom:1.1em;
}
.eyebrow::before{
  content:"";
  width:28px; height:1px;
  background:var(--brass);
  display:inline-block;
}

.container{
  max-width:var(--container);
  margin:0 auto;
  padding:0 clamp(20px, 5vw, 56px);
}

.visually-hidden{
  position:absolute !important;
  width:1px; height:1px;
  overflow:hidden;
  clip:rect(0 0 0 0);
  white-space:nowrap;
}

/* ---------- Buttons ---------- */
.btn{
  display:inline-flex;
  align-items:center;
  gap:0.55em;
  font-family:var(--font-body);
  font-weight:600;
  font-size:0.92rem;
  letter-spacing:0.01em;
  padding:0.95em 1.7em;
  border-radius:var(--radius);
  border:1px solid transparent;
  cursor:pointer;
  transition:background 0.25s ease, color 0.25s ease, border-color 0.25s ease, transform 0.2s ease;
}
.btn:focus-visible{ outline:2px solid var(--brass); outline-offset:3px; }
.btn-primary{ background:var(--olive); color:var(--cream); }
.btn-primary:hover{ background:var(--olive-dark); transform:translateY(-1px); }
.btn-ghost{ background:transparent; color:var(--ink); border-color:var(--line); }
.btn-ghost:hover{ border-color:var(--ink); }
.btn-on-dark{ background:var(--brass); color:var(--ink); }
.btn-on-dark:hover{ background:#c99a5f; }
.btn-arrow{ transition:transform 0.25s ease; }
.btn:hover .btn-arrow{ transform:translateX(3px); }

/* ---------- Header ---------- */
.site-header{
  position:fixed; top:0; left:0; right:0;
  z-index:100;
  padding:18px 0;
  transition:background 0.35s ease, padding 0.35s ease, box-shadow 0.35s ease, border-color .35s ease;
  border-bottom:1px solid transparent;
}
.site-header.is-scrolled,
.site-header.is-solid{
  background:rgba(239,234,222,0.92);
  backdrop-filter:blur(10px);
  padding:12px 0;
  border-bottom-color:var(--line);
}
.header-inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
}
.logo{
  font-family:var(--font-display);
  font-size:1.25rem;
  font-weight:600;
  letter-spacing:0.02em;
  color:var(--ink);
  white-space:nowrap;
}
.logo span{ color:var(--brass-dark); font-style:italic; font-weight:500; }

.site-nav{
  display:flex;
  align-items:center;
  gap:2.2rem;
}
.site-nav a{
  font-size:0.86rem;
  font-weight:500;
  letter-spacing:0.03em;
  text-transform:uppercase;
  position:relative;
  padding:0.3em 0;
}
.site-nav a::after{
  content:"";
  position:absolute; left:0; right:100%; bottom:0;
  height:1px; background:var(--brass-dark);
  transition:right 0.25s ease;
}
.site-nav a:hover::after,
.site-nav a[aria-current="page"]::after{ right:0; }
.site-nav a[aria-current="page"]{ color:var(--brass-dark); }

.header-right{ display:flex; align-items:center; gap:1.4rem; }

.lang-switch{ display:flex; gap:2px; font-size:0.74rem; font-weight:700; letter-spacing:0.04em; }
.lang-switch button{
  background:transparent; border:1px solid var(--line);
  color:var(--ink-soft); padding:0.4em 0.65em; cursor:pointer;
  font-family:var(--font-body);
}
.lang-switch button + button{ border-left:none; }
.lang-switch button.is-active{ background:var(--olive); color:var(--cream); border-color:var(--olive); }

.nav-toggle{
  display:none;
  background:none; border:none; cursor:pointer;
  width:32px; height:24px; position:relative;
}
.nav-toggle span, .nav-toggle span::before, .nav-toggle span::after{
  content:""; position:absolute; left:0; right:0; height:2px; background:var(--ink);
  transition:transform 0.25s ease, opacity 0.25s ease, top 0.25s ease;
}
.nav-toggle span{ top:11px; }
.nav-toggle span::before{ top:-8px; }
.nav-toggle span::after{ top:8px; }
.nav-toggle[aria-expanded="true"] span{ background:transparent; }
.nav-toggle[aria-expanded="true"] span::before{ top:0; transform:rotate(45deg); }
.nav-toggle[aria-expanded="true"] span::after{ top:0; transform:rotate(-45deg); }

@media (max-width: 860px){
  .nav-toggle{ display:block; order:3; }
  .site-nav{
    position:fixed; inset:0 0 0 auto; top:0;
    width:min(320px, 84vw); height:100vh;
    background:var(--cream);
    flex-direction:column; align-items:flex-start;
    padding:110px 32px 40px;
    gap:1.6rem;
    transform:translateX(100%);
    transition:transform 0.35s ease;
    box-shadow:-10px 0 30px rgba(0,0,0,0.15);
  }
  .site-nav.is-open{ transform:translateX(0); }
  .site-nav a{ font-size:1.1rem; }
  .header-right{ order:2; }
}

/* ---------- Hero ---------- */
.hero{
  position:relative;
  min-height:92vh;
  display:flex;
  align-items:flex-end;
  padding:150px 0 90px;
  overflow:hidden;
  background:var(--olive-dark);
  color:var(--cream);
}
.hero::before{
  content:"";
  position:absolute; inset:0;
  background:
    radial-gradient(ellipse 80% 60% at 85% 15%, rgba(184,135,74,0.22), transparent 60%),
    linear-gradient(155deg, #232b1c 0%, #1c2116 55%, #171a12 100%);
}
.hero::after{
  content:"";
  position:absolute; inset:0;
  opacity:0.5;
  background-image:repeating-linear-gradient(115deg, rgba(239,234,222,0.035) 0px, rgba(239,234,222,0.035) 1px, transparent 1px, transparent 68px);
}
.hero-inner{ position:relative; z-index:2; max-width:820px; }
.hero-motif{
  position:absolute; z-index:1; right:-40px; top:18%;
  width:min(46vw, 520px); opacity:0.5;
  pointer-events:none;
}
@media (max-width:860px){ .hero-motif{ display:none; } }
.hero .eyebrow{ color:var(--brass); }
.hero .eyebrow::before{ background:var(--brass); }
.hero h1{ color:var(--cream); }
.hero p.lede{ color:rgba(248,246,240,0.85); font-size:1.15rem; max-width:620px; }
.hero-actions{ display:flex; flex-wrap:wrap; gap:1rem; margin-top:1.8rem; }

.page-hero{
  padding:150px 0 70px;
  border-bottom:1px solid var(--line);
}
.page-hero .eyebrow{ margin-bottom:0.8em; }
.page-hero p.lede{ max-width:640px; font-size:1.08rem; }

/* ---------- Sectors strip ---------- */
.sectors{
  border-top:1px solid var(--line);
  border-bottom:1px solid var(--line);
  padding:1.3rem 0;
}
.sectors ul{
  list-style:none; margin:0; padding:0;
  display:flex; flex-wrap:wrap; gap:2rem;
  justify-content:center;
  font-size:0.8rem; letter-spacing:0.12em; text-transform:uppercase;
  color:var(--ink-soft); font-weight:600;
}

/* ---------- Section rhythm ---------- */
section{ padding:6.5rem 0; }
.section-tight{ padding:4rem 0; }
.section-header{ max-width:680px; margin-bottom:3rem; }
.section-split{
  display:grid; grid-template-columns:0.9fr 1.1fr; gap:4rem; align-items:start;
}
@media (max-width:860px){
  .section-split{ grid-template-columns:1fr; gap:2rem; }
}

.bg-cream{ background:var(--cream); }
.bg-ink{ background:var(--ink); color:var(--stone); }
.bg-ink h2, .bg-ink h3{ color:var(--stone); }
.bg-ink p{ color:rgba(239,234,222,0.78); }
.bg-ink .eyebrow{ color:var(--brass); }
.bg-ink .eyebrow::before{ background:var(--brass); }

/* ---------- Menu-style list (signature) ---------- */
.menu-list{ list-style:none; margin:0; padding:0; }
.menu-item{
  display:grid; grid-template-columns:70px 1fr auto;
  gap:1.6rem; align-items:baseline;
  padding:1.7rem 0;
  border-top:1px solid var(--line);
}
.menu-list .menu-item:last-child{ border-bottom:1px solid var(--line); }
.menu-num{
  font-family:var(--font-display); font-style:italic; font-weight:500;
  color:var(--brass-dark); font-size:1.05rem;
}
.menu-title{ font-family:var(--font-display); font-size:1.3rem; font-weight:600; margin:0 0 0.3em; }
.menu-desc{ color:var(--ink-soft); margin:0; font-size:0.96rem; max-width:640px; }
.menu-link{
  font-size:0.8rem; font-weight:600; letter-spacing:0.05em; text-transform:uppercase;
  white-space:nowrap; align-self:center; color:var(--olive);
}
.menu-item:hover .menu-title{ color:var(--brass-dark); }
@media (max-width:640px){
  .menu-item{ grid-template-columns:40px 1fr; }
  .menu-link{ grid-column:2; }
}

/* ---------- Cards ---------- */
.card-grid{ display:grid; grid-template-columns:repeat(3, 1fr); gap:2rem; }
@media (max-width:960px){ .card-grid{ grid-template-columns:repeat(2, 1fr); } }
@media (max-width:640px){ .card-grid{ grid-template-columns:1fr; } }

.pillar-card{
  background:var(--cream);
  border:1px solid var(--line);
  padding:2rem 1.8rem;
}
.pillar-card .pillar-num{
  font-family:var(--font-display); font-style:italic; color:var(--brass-dark);
  font-size:0.95rem; margin-bottom:0.9rem; display:block;
}
.pillar-card h3{ margin-bottom:0.5em; }
.pillar-card p{ font-size:0.94rem; margin-bottom:0; }

/* ---------- Process steps ---------- */
.process{ display:grid; grid-template-columns:repeat(3,1fr); gap:2.5rem; }
@media (max-width:860px){ .process{ grid-template-columns:1fr; } }
.process-step{ border-top:2px solid var(--brass); padding-top:1.3rem; }
.process-step .step-num{ font-family:var(--font-display); font-style:italic; font-size:1.6rem; color:var(--brass-dark); }

/* ---------- Service detail (services.html) ---------- */
.service-block{
  display:grid; grid-template-columns:120px 1fr; gap:2.5rem;
  padding:3.4rem 0; border-top:1px solid var(--line);
}
.service-block:last-child{ border-bottom:1px solid var(--line); }
.service-block .menu-num{ font-size:2.4rem; }
.service-block h3{ margin-bottom:0.6rem; }
.service-block p{ max-width:680px; }
@media (max-width:700px){
  .service-block{ grid-template-columns:1fr; gap:0.8rem; }
  .service-block .menu-num{ font-size:1.4rem; }
}

/* ---------- CTA band ---------- */
.cta-band{
  background:var(--olive);
  color:var(--cream);
  text-align:center;
  padding:5rem 0;
}
.cta-band h2{ color:var(--cream); max-width:720px; margin:0 auto 0.6em; }
.cta-band p{ color:rgba(248,246,240,0.82); max-width:560px; margin:0 auto 1.8em; }

/* ---------- About photo strip ---------- */
.photo-strip{ display:grid; grid-template-columns:repeat(4,1fr); gap:1rem; }
.photo-strip img{ width:100%; height:220px; object-fit:cover; filter:grayscale(15%) sepia(6%); }
@media (max-width:860px){ .photo-strip{ grid-template-columns:repeat(2,1fr); } }

/* ---------- Contact ---------- */
.contact-grid{ display:grid; grid-template-columns:1fr 1fr; gap:4rem; align-items:start; }
@media (max-width:860px){ .contact-grid{ grid-template-columns:1fr; } }
.contact-info dt{ font-size:0.75rem; letter-spacing:0.1em; text-transform:uppercase; color:var(--brass-dark); font-weight:700; margin-top:1.6rem; }
.contact-info dd{ margin:0.3em 0 0; font-size:1.05rem; }
.social-row{ display:flex; gap:1rem; margin-top:1.6rem; }
.social-row a{
  border:1px solid var(--line); padding:0.6em 1.1em; font-size:0.82rem; font-weight:600;
}
.social-row a:hover{ border-color:var(--olive); color:var(--olive); }
.map-frame{ width:100%; height:280px; border:1px solid var(--line); margin-top:2rem; filter:grayscale(25%); }

.form-field{ margin-bottom:1.3rem; }
.form-field label{ display:block; font-size:0.78rem; letter-spacing:0.06em; text-transform:uppercase; font-weight:600; margin-bottom:0.5em; color:var(--ink-soft); }
.form-field input, .form-field textarea{
  width:100%; padding:0.85em 1em; border:1px solid var(--line);
  background:var(--cream); font-family:var(--font-body); font-size:0.98rem; color:var(--ink);
}
.form-field textarea{ min-height:140px; resize:vertical; }
.form-field input:focus, .form-field textarea:focus{ outline:2px solid var(--brass); outline-offset:1px; }
.form-note{ font-size:0.82rem; color:var(--ink-soft); margin-top:0.8rem; }

/* ---------- Footer ---------- */
.site-footer{ background:var(--ink); color:rgba(239,234,222,0.8); padding:4.5rem 0 2rem; }
.footer-grid{ display:grid; grid-template-columns:1.4fr 1fr 1fr; gap:3rem; padding-bottom:3rem; border-bottom:1px solid rgba(239,234,222,0.14); }
@media (max-width:760px){ .footer-grid{ grid-template-columns:1fr; gap:2.2rem; } }
.footer-logo{ font-family:var(--font-display); font-size:1.3rem; color:var(--stone); margin-bottom:0.8rem; }
.footer-logo span{ color:var(--brass); font-style:italic; }
.footer-col h4{ color:var(--stone); font-family:var(--font-body); font-size:0.78rem; letter-spacing:0.1em; text-transform:uppercase; margin-bottom:1rem; }
.footer-col a, .footer-col p{ display:block; font-size:0.92rem; margin-bottom:0.65em; color:rgba(239,234,222,0.72); }
.footer-col a:hover{ color:var(--brass); }
.footer-bottom{ display:flex; justify-content:space-between; flex-wrap:wrap; gap:0.8rem; padding-top:1.6rem; font-size:0.78rem; color:rgba(239,234,222,0.55); }

/* ---------- Reveal-on-scroll ---------- */
[data-reveal]{ opacity:0; transform:translateY(18px); transition:opacity 0.7s ease, transform 0.7s ease; }
[data-reveal].is-visible{ opacity:1; transform:translateY(0); }

/* ---------- Skip link ---------- */
.skip-link{
  position:absolute; left:-999px; top:0; background:var(--olive); color:var(--cream);
  padding:0.8em 1.2em; z-index:1000;
}
.skip-link:focus{ left:12px; top:12px; }
